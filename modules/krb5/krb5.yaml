---
document: modulemd
version: 1
data:
    summary: The Kerberos network authentication system
    description: Kerberos V5 is a trusted-third-party network authentication
                 system, which can improve your network's security by
                 eliminating the insecure practice of sending passwords over
                 the network in unencrypted form.
    license:
        module: [MIT]
    dependencies:
        buildrequires:
            bootstrap: master
        requires:
            platform: master
            platform-placeholder: master
    references:
        community: https://docs.pagure.org/modularity/
        documentation: https://github.com/modularity-modules/krb5
        tracker: https://github.com/modularity-modules/krb5
    profiles:
        default:
            rpms:
                - krb5-workstation
    api:
        rpms:
            - krb5-libs
            - krb5-workstation
    components:
        rpms:
            words:
                rationale: Generated.
                ref: f27
